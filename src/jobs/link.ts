import prisma from '../db/prisma';

export async function createLinks(memoryId: string, vector: Buffer) {
  // Placeholder: find nearest neighbors and create semantic links
  console.log(`Linking memory ${memoryId}`);
}
